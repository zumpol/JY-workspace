#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#define _CRT_MONSTDC_NO_DEPRECATE

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <string.h>
#include <ctype.h>
#include <limits.h>

int main() {
	int A, B;
	while (scanf("%d %d", &A, &B) == 2){ // The loop continues if two integers are successfully read.
		printf("%d\n", A + B);
	}
	return 0;
}
