t#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#define _CRT_MONSTDC_NO_DEPRECATE

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <string.h>
#include <ctype.h>
#include <limits.h>

int main() {
	char S[1000];
	int i;
	scanf("%s %d", S, &i); // Omit '&' for string input to avoid pointer errors.
  
	printf("%c", S[i - 1]);
  return 0;
}
