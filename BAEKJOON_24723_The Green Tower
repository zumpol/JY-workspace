#define _CRT_SECURE_NO_WARNINGS
#define _USE_MATH_DEFINES
#define _CRT_MONSTDC_NO_DEPRECATE

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <string.h>
#include <ctype.h>
#include <limits.h>

int main() {
	int N;
	scanf("%d", &N);
	printf("%d\n", (int)pow(2, N)); // Formula : 2 ^ N -> Because Each level doubles the paths. 
	return 0;
}
